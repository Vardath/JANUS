<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:name=".JanusApplication"
        android:allowBackup="true"
        android:label="JANUS v0.80"
        android:usesCleartextTraffic="false"
        android:theme="@style/AppTheme">
        <activity android:name=".SettingsActivity" android:label="JANUS Options" android:exported="false" />
        <activity android:name=".MaintenanceActivity" android:label="JANUS Maintenance" android:exported="false" />
        <activity android:name=".ResearchActivity" android:label="JANUS Research" android:exported="false" />
        <activity android:name=".ArtifactActivity" android:label="JANUS Artifacts" android:exported="false" />
        <activity android:name=".MainActivity" android:label="JANUS Chat" android:exported="false" />
        <activity android:name=".HomeActivity" android:label="JANUS v0.80" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.files"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
