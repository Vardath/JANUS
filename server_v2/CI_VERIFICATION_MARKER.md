# JANUS server v2 CI verification marker

No runtime behaviour. This branch mirrors current main plus this marker so pull-request CI can verify the exact clean-server snapshot before production cutover.
